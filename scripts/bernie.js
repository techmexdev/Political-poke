var bernie(){
    function burn(){
      var rand = Math.floor((Math.random()*10 ) +1);
      if(rand==1){
        document.getElementById('message').innerHTML = "Bernie's attack missed!"
      }
      else {
        playerHP=playerHP-8
      }
    };
    function twitterSupport(){
      var rand = Math.floor((Math.random()*10 ) +1);
      if(rand==1){
        document.getElementById('message').innerHTML = "Bernie's attack missed!"
      }
      else {
        playerHP=playerHP-8
        document.getElementById('message').innerHTML = "Bernie's attack missed!"
      }
    };
    function tuiton(){
      var rand = Math.floor((Math.random()*10 ) +1);
      if(rand==1){
        document.getElementById('message').innerHTML = "Bernie's attack missed!"
      }
      else {
        playerHP=playerHP-8
        document.getElementById('message').innerHTML = "Bernie's attack missed!"

      }
    };
    function fingerOfJustice(){
      var rand = Math.floor((Math.random()*10 ) +1);
      if(rand==1){
        document.getElementById('message').innerHTML = "Bernie's attack missed!"
      }
      else {
        playerHP=playerHP-8
        document.getElementById('message').innerHTML = "Bernie's attack missed!"
      }
    };
}
